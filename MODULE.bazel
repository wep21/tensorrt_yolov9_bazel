module(name = "tensorrt_yolox")

bazel_dep(name = "rules_cc", version = "0.0.9")

non_module_deps = use_extension("@tensorrt_yolox//:extensions.bzl", "non_module_deps")
use_repo(
    non_module_deps,
    "opencv",
)
